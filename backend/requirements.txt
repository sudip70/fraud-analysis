fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.3
# xgboost is optional in the pipeline (guarded by try/import) but included here
# so Render always has it available. Remove if build time or disk space is a concern.
xgboost>=2.0
shap>=0.44
python-multipart>=0.0.9
tqdm>=4.65
# FastAPI (>=0.110) expects Pydantic v2.
pydantic>=2.7,<3
